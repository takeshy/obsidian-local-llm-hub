// The edit-history browser lives in the shared library; class names follow this
// plugin's prefix through the shared cls() helper.
export { EditHistoryModal } from "obsidian-llm-hub-common/modals";
